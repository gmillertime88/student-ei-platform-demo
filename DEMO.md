# DEMO.md — Student EI Platform Demo, Functional Reference

**What this is:** the screen-by-screen reference for `index.html` as built. A new Claude session (or any developer) reads this plus `STATUS.md` at the client root before changing anything.

**Revision Round 1 complete (2026-08-22, register: `Working Files/StudentEI_Change Register R1 v2 - 2026-08-22_GM.md`).** CP1 (foundation) and CP2 (IA restructure) are committed: "Skills Builder" terminology, no at-risk/absenteeism/career content anywhere, new logo, MAV input at top, Oct 2026 cycle, new GP/credential/GPA data model; sidebar reduced to Students / Credentials / Benchmarks / Console; Assessments, Surveys, Frameworks, Graduate Profile tabs DELETED (routes redirect to Home); all career/survey/framework content and MAV career prompts removed permanently (roster `vertical` field stripped); My Interests deleted from the student experience; Home rebuilt (trend + GP modules); Console rebuilt (Assessment Management + Resources & Research + Platform Tutorial Videos stubs; Survey/Credential Creator tiles deleted); New Chat and Chats re-scripted. All checkpoints are committed and the demo document set is revved to **v9** (2026-08-22): Demo Script v9 (Prototype/), Prototype Requirements v9 (Requirements/), Demo Screens v9 (Design & Mockups/). Round 1 was pushed and is live.

**Revision Round 2 in progress (2026-08-25, source: `Working Files/StudentEI_District Dashboard Edits to R1 Build (from Tyler) - 2026-08-24.docx`).** The edits doc is BUILT: home language selector, i-icon fix, trend Clear all, Graduate Profile module full-width with cohort chips, MAV thread fixes (fresh conversation per card, newest-first, page-top landings, Home resets defaults), Students skills dropdown drives the column, benchmark rename + per-benchmark `skill`, console template form + back links + whitepaper link, student durable-skills relabel (`sdBucket` proficiency at line end) + nav badge, Credentials **Percentage of District Goal** module, **Create a Skills Builder** rebuilt to Tyler's mock, My Library subject rows, student **dual Skills Builder queue** (English + Chemistry). The companion `StudentEI_Credential Subject Updates (from Tyler) - 2026-08-24.docx` is also BUILT (GM approved 2026-08-25): the four PT-drafted subjects are replaced by Tyler's verbatim activities (Math = Algebra I Linear Equations Problem-Solving, Social Studies = Global 10 Global Issues Debate, CTE = Nursing Pathway Patient Care Simulation, Other = Community Volunteering Project; Draft badges retired), and every subject tab now opens with its **learning-standards list** (`SB_STANDARDS` / `SB_STANDARDS_CTE`, Tyler's rows verbatim; English Speaking/Presentation and Science Lab/Laboratory Investigation carry live Create buttons that open the built English 10 / Chemistry entries; the CTE list is 11 career areas × 3 activities in a capped-height scroll). QA: 153 checks green. Doc set revs to v10 when Round 2 closes.

**UX pass (2026-08-25, GM-directed, outside Tyler's lists).** App shell rebuilt as a proper dashboard frame: sidebar is a pinned 216px rail (`position:sticky`, own scroll) that auto-collapses to 72px below 1440 unless the presenter toggles it; the topbar is sticky at 88px with the logo at 56px (48px below 1360) so the mark's second line is legible, and the decorative URL chip hides below 1600 so the breadcrumb survives; the **MAV panel is now a viewport-height rail** (`sticky`, `calc(100vh - var(--topbar-h))`) that scrolls internally, which closed a 1,343px dead gap between the insights and the Ask MAV prompts (it previously grew to full *page* height with `margin-top:auto` pushing the prompts to the bottom). Content column at 1280 went from 656px to 824px, which un-wrapped the Students table. **Scroll behavior fixed:** `render()` only resets scroll when the route key actually changes, and it restores `scrollY` after the `#view` innerHTML swap (which momentarily collapses document height and makes the browser clamp the scroll); MAV click-throughs still land at the top by clearing `lastRouteKey` before their action runs. Also: trend and PDG chips now carry CYCLE/CLASS/MONTH axis labels on their own rows, the New Chat screen hides the duplicate panel prompt list (`mavChipsWrap`), the Students filter grid lost its redundant second "Clear all", proficiency range labels shrank so they stop colliding in narrow cards, identity columns are `white-space:nowrap` with tighter gutters below 1560, and the sign-on logo went 40px to 62px. QA: **168 checks green.**

**Spanish, student experience (2026-08-26, GM-directed).** The language selector is a single control in the header, in the same position for every role (GM 2026-08-26: Tyler's original inline selector on the Home page title was removed so the control never moves between roles or screens; the "Coming Soon" pill renders to its left so the select does not shift when it appears). Behavior is role-dependent by design: **the student experience is fully functional in Spanish; every staff role shows a "Coming Soon" pill instead** (that exact wording, nothing appended) and stays in English. Header scaled a further 20% to 106px with the logo at 67px (58px below 1360). Implementation is a **presentation-layer translation over the rendered DOM** (`I18N_ES`, 370+ entries, plus 8 regex patterns for interpolated strings like "240 of 500" and "question 1 of 5"): `applyLang()` walks the text nodes of `#view`, `#mavPanel`, `#crumbScreen`, the sidebar, and the registration overlay after each render, and `showToast` routes through `i18nToastText`. Data keys are deliberately untouched — `COMPS`, `MAV_SCRIPTS` keys, tally lookups and `SKILL_PARENT` all stay English, so no data-model path can break on a language switch. Static regions keep their English source in a `WeakMap` so switching back restores exactly. `applyRole()` resets both language states, and a language switch clears the MAV thread so the conversation starts in the new language. Coverage: hub, durable skills, credentials, feedback, new chat, chats, welcome, the 92-item assessment sampler, the registration overlay, both Skills Builder question sets, student MAV replies, insights, chips, and toasts. Layout was re-checked for Spanish text expansion: nav items now wrap (`min-height`, `line-height:1.25`) instead of colliding. **Also fixed a real data-appropriateness bug found during this pass:** `student-welcome` and `student-assessment` had no `insights`/`chips` and fell back to `DEFAULT_INSIGHTS`, so a student saw district aggregates ("116 of 1,284 students", cohort comparisons). Both routes now carry student-appropriate content, and `STUDENT_DEFAULT_INSIGHTS`/`STUDENT_DEFAULT_CHIPS` are the role-aware fallback. New MAV script: `what are durable skills?`. QA: **193 checks green**, including a leak check that no student route surfaces district aggregates.

**Revision Round 3 (2026-08-26, source: Tyler's re-marked edits doc + notes dated 8/26).** Tyler struck 60 items as complete and left 8 open. Built: **one proficiency scale.** The demo had been running two — `rangeOf(score)` behind benchmarks, filters and MAV, and a separate 20%-band read of the line width behind the Skills Summary label — so Devon read "Developing in Adaptability" on the benchmark and "Advancing" on his own report. `rangeOf()` is now the single source of truth for the word, and `bandWidth(score)` derives the line: it lands `band_start + 2..6` so the label that follows sits inside the same 20% column, capping at 90% per Tyler's note. Every non-Sofia student's line is score-derived, so a roster range and a report label can no longer disagree. Sofia keeps her authored profile with widths moved off the column boundaries (GM option A, 2026-08-26: line position wins, so Creativity reads Emerging rather than the originally authored Developing); May 2026 is now 28/48/25/68/46 and October 2025 24/44/22/46/27, and `TREND_W` on the district home follows the same convention. Verified: 130 of 130 rows across all 24 students and both student cycles have the line end and the label in one column. **Staff student view now mirrors the student**: the duplicate profile-head card is gone, `sdHeadCard`'s three-column layout (identity, credential tallies with the goal ring, Graduate Profile alignment) is shared via `goalRing()`/`studentIdOf()`/`studentEmailOf()`, status/top-skill/assessed-date fold into the identity column, the cycle chips became the student's dropdown, and the Skills Summary is full width (which is also what makes the labels fit). Also: the "continue your durable skills summary" banner now appears on My Durable Skills while the summary is unfinished; "Portrait of a Graduate ranges" removed from the Skills Summary corner; the "Benchmarks re-evaluate against current data each time they open" sentence deleted; and all five Console sub-pages (User Management, Assessment Management, Resources, Tutorial Videos, SIS) now carry the identical "← Console" back link above the title. No action on two items Tyler resolved himself: the Clear All placement he marked "OKAY AS IS", and My Credentials which he wants to discuss live. QA: **203 checks green.** **Follow-up (GM):** the per-row "Show all 4" / "Show current" text link on the Competency Trend became a caret toggle sitting beside that row's i-icon — down caret expands the row to all four assessment cycles, up caret (solid, filled) collapses it back to the current cycle. `data-trend-all` and `trendRowAll` state are unchanged; only the control is new (`.caret-btn`, `ICO.caretDown`/`ICO.caretUp`, `aria-expanded`). QA: **209 checks green.**

**R4 (2026-08-26, GM list of five).** (1) The Graduate Profile Alignment tile on Home and the Students Past Goal tile on Credentials render their values in the same `--n900` black as the other tiles; the blue inline colour is gone. (2) **Graduate Profile now declines by graduating class.** `GP_CLASS_ADJ` (additive, with 2029 arbitrarily lowest) is replaced by `GP_CLASS_FACTOR` — 2027 strongest, each later class 5 to 10 percent below the one before it (6.9 / 6.0 / 9.0 / 4.9 / 8.1 / 6.1), normalised so the seven classes average to the published district figure and "All Students" stays honest. Home module by class: 83 / 78 / 73 / 66 / 63 / 58 / 54. The same curve drives `gpOf()` per student (so the Students table GP column and the student header agree), the credentials Percentage of District Goal module, `GP_CLASS_LOW` (now Class of 2033 at 54%), and the MAV copy that named the weakest classes (now 2032 and 2033). Sofia stays exempt on Tyler's printed numbers. (3) `.trend-ranges` labels are centred over their columns instead of left-aligned. (4) **Spanish revert bug fixed:** the MAV panel's static labels ("Ask MAV" above the prompt chips, "Current View") and the input placeholder were translated in place and never restored, so switching back to English left them Spanish until a refresh. `#mavPanel` is now a remembered region restored symmetrically, and attribute originals are tracked in `I18N_ATTR`. The MAV input placeholder is deliberately excluded and set from the role in `applyLang()`, since it is role-owned ("about your skills" vs "about your district") rather than language-owned. (5) Home tile labels no longer wrap: `white-space:nowrap` plus stepped type at 1700 / 1500 / 1380px, verified single-line from 1280 to 1600. QA: **221 checks green.**

**R5 (2026-08-28, Tyler "Edits post 8/26 meeting" + GM dispositions).** (1) **Home cycle chips:** Current is uncheckable only while it is the *sole* checked cycle. With another cycle checked, Current can be unchecked; unchecking the last remaining cycle re-checks Current automatically. (2) **Current Assessment Cycle card deleted from Home** and replaced, per the second half of Tyler's own item ("delete that and duplicate/add what is shown below in Student tab, 3 and 4"), by a **district-wide Awaiting Verification queue** (`homeAwaitingVerification()`): the six newest pending submissions across the district, one row per student and no repeated activity, each row clicking through to that student's record, a count pill of the district total, and a "Review all in Students" button that applies the Awaiting Verification filter — which closes Tyler's loop from Home to the filter to the student record. Home now runs metric tiles → Competency Trend → Graduate Profile → Awaiting Verification → Explore Modules. (3) **Students filter:** "Awaiting Verification" is the top option in the Credentials dropdown, above the five competencies. Selecting it narrows the directory to students with pending submissions (`avCount(s) > 0`) and the Credentials column reads "Awaiting Verification (n)". Wired through `filteredStudents`, the filter chips, Create-a-Benchmark naming/criteria and the MAV benchmark snapshot. (4) **Awaiting Verification module** replaces the Credential Progress card on the *staff* student record (`#/student/{id}` only — the student's own dashboard is unchanged). Library-list styling to Tyler's mock without the colour and fluff: tabs Awaiting Verification (n) / Accepted (n) / Add Feedback; columns Activity-Experience · Submitted By · Submitted · Source · Status · Verified; sub-skill tags under each activity; pagination footer. **Visual only — no verification workflow.** (5) **Student dashboard:** the assessment-history dropdown moved out of the page head into the Skills Summary card header, matching the staff view. (6) **My Feedback / saved chats:** the lifeguard example is replaced everywhere (transcript, chat title, both Spanish dictionary entries) with the ice cream stand copy Tyler supplied. QA: **257 checks green.**

**R6 (2026-09-03, Tyler "2 Edits" doc from his BOCES superintendent meeting).** (1) The Skills Builder library now opens on **My Library** instead of English. (2) **"Tallies" retired** and replaced with "Credential Progress" on both the staff and student headers, plus the Spanish entry (Tyler: the term is frowned on in K-12). (3) New **Educator Input** tab at the end of the library tabs. (4) **The 2 / 1 / 0 scoring grid** (`eduInputModule`): ten students, one row each, scored against a Skills Builder. **Five competency columns by default so it fits a laptop, with a caret on each column that drills into that competency's four sub-skills** (`EDU_SUB`, drawn verbatim from `COMP_INFO.subskills`), which roll up to the parent via `eduParentOf` and `eduScored`. This resolves a contradiction in Tyler's reply, where he asked for five columns and also for teachers to be able to rate work ethic and attention to detail; the caret gives both without widening the table. One value per student per skill, clicking a checked box clears it, and a per-row "All 2 / 1 / 0 / ×" control scores or clears a whole row. Visual only, no submission. **Attention to Detail now maps to Self-Management** per Tyler. (5) **Credential Points by Contributor** (`credSplit`, `credSplitModule`) on the district user's view of a student only, never the student's own dashboard: student, educator and other columns per competency plus a total row. Shares are deterministic and **always sum to `credTally()`**, so every figure already published elsewhere is unchanged; Tyler confirmed his example numbers were placeholders ("that was me plugging in random numbers as visuals"). (6) **Home recomposed:** the Awaiting Verification queue is removed (his superintendent sees verification as a counselor task and wants durable skills, Portrait and credentials on the landing page), the **Percentage of District Goal** module takes its place, and Graduate Profile moves below it. `homeAwaitingVerification()` and all its helpers are kept intact so one call in `viewHome()` restores it. Class periods were considered and dropped at Tyler's direction. QA: **279 checks green.**

**Not translated by design:** the sign-on screen (staff portal, pre-role), all staff screens, proper nouns, and the product names Student EI, MAV, and Skills Builder. The full product needs real i18n (locale files, ICU messages, es-US/es-MX variants) and, for the assessment instrument specifically, psychometric re-validation rather than translation alone.

**Maintenance rule:** update this file **in the same commit** as any demo change. If a screen, route, MAV prompt, or behavior changes and this file doesn't, the commit isn't done.

**Read order for a new session:** client `CLAUDE.md` → client `STATUS.md` (invariants, standing directives, current state) → this file → the code sections you're changing.

---

## 1. Architecture

- **Single self-contained file:** `index.html` (~300KB). All CSS, JS, data, and the Student EI logo (data URI) inline. No build step, no dependencies, works from `file://` and Cloudflare Pages.
- **Hash router:** `location.hash` → `currentRoute()` → `render()` looks up `ROUTES[name]`, calls the view function, injects into `#view`, then wires events via `wireView()` + `wireStudentViews()`. Sub-routes use a second segment (`#/student/4`, `#/surveys/healthcare`, `#/assessments/samples`, `#/credentials/checkin`, `#/benchmarks/0`, `#/console/users`).
- **Login-first:** no hash or `#/signon` shows the sign-on overlay; nothing renders until a persona signs in. Sign out returns to sign-on.
- **Deterministic and front-end only:** every number, list, and MAV answer is scripted or derived from the inline data. No network calls.
- **Deploy:** git push to `main` → GitHub `gmillertime88/student-ei-platform-demo` → Cloudflare Pages behind Cloudflare Access. Runbook: `Cloudflare-GitHub-Deploy-Cheat-Sheet.md`.

## 2. Roles and access

Four personas, chosen on the sign-on screen (`ROLES`, `applyRole()`):

| Persona | Role | Sees | Console | Notes |
|---|---|---|---|---|
| Dr. Dana Whitfield | Superintendent | All 24 roster students, all screens | Full | No banner |
| Michael Grant | Principal, Jasper HS | Jasper HS students only | Full | Permission banner; Jasper MS records hidden |
| James Petrillo | CTE Teacher | 6 CTE students (ids 3,4,6,10,12,14) | None | Banner; district totals stay visible as aggregates |
| Sofia Nguyen | Student, Class of 2028 | Her own record only (id 4) | None | Student experience only (section 4) |

- **Hidden records are fully hidden** with count-only disclosure; a direct link to a restricted `#/student/{id}` shows a lock screen (deliberate REQ-F-014 deviation).
- **Role guards:** `STUDENT_ROUTES` set boxes the student persona into the student routes and boxes staff out of them. Console requires `console: "full"`.
- Sign-on also carries the student registration entry point ("New student? Create your account" → `#/student-register`).

## 3. District screens

| Route | Screen | What it shows / does |
|---|---|---|
| `#/home` | District Overview | 4 metric tiles (Total Students 1,284 w/ 1,168 Verified · Assessment Completion 91% · Graduate Profile Alignment 68% · Credential Points Awarded 318,540); **Competency Trend module** (`homeTrend()` — Tyler-spec line widths 40/60/30/80/50, nothing at line ends, range columns Developing→Mastering, cycle chips Current/May 2026/Oct 2025/May 2025 overlay newest-on-top (R5: Current is locked only when it is the only checked cycle, and re-checks itself when the last other cycle is cleared), per-class chips **on one line with a Clear all button** (R2 — All Students chip removed; Clear all restores the default), per-row caret expand/collapse beside the i-icon, i-icon definitions (R2: CSS fix — `.bar-name` no longer clips the button); state `trendCycles`/`trendYear`/`trendRowAll`); **Graduate Profile module** (R2: full width, Recent Skills Builders card deleted; `homeGradProfile()` — `GP_DIST` 5 green + 3 blue district-added, 68% overall, **All Students + 2027-2033 cohort chips** (`gpTrendYear`, `GP_CLASS_ADJ` shifts every line per class), callouts: Class of 2029 growth + Competency for District Review = lowest of the 8 — Creativity, confirmed by Tyler 2026-08-24); **display-language select** (English/Español, static talking point) beside the page title; **district-wide Awaiting Verification queue** (R5 — replaced the deleted Current Assessment Cycle card: 6 newest pending submissions, one per student, rows navigate to the student record, "Review all in Students" applies the filter); Explore Modules tiles (Students/Credentials/Benchmarks/Console) |
| `#/new-chat` | New Chat | MAV chat page with suggestion cards; results open in the content area |
| `#/chats` | Chats | MAV conversation history; selecting one replays its prompts against current data |
| `#/students` | Students | Directory (24-row sample of 1,284). Filter bar (R1 D1): Search · Status (**Active default**; Pending/Archived/All) · Grad Year · School · **Assessment date** (Oct 2026 default; past cycles shift Skills/Proficiency via `scoresFor`) · Skills (R2: selecting a competency fills the whole Skills column with it — also what MAV-filtered lists show, so the column matches the prompt) · Proficiency · **Credentials** (R5: **Awaiting Verification** first, narrowing to students with pending submissions and relabelling the Credentials column; then top-tally competency) · **Graduate Profile** (All or one of 8 — switches the GP column to that competency) · sort · Clear all. "Create Benchmark" button opens the Lite dialog (`cbOpen`) pre-filled from active filters; Save appends a session-only row to `BENCHMARKS` with a criteria snapshot. Columns: Name · Grad Year · School · Skills · Credentials (top tally x/500) · Graduate Profile % |
| `#/student/{id}` | Student Dashboard (staff view) | R1 E1/E3 rebuild: identity head + assessment hint; **header card** with GPA (`gpaOf`) · Credential Tallies (5 × x/500, goal 375) · Graduate Profile Alignment (8 comps, district-added shown blue, goal 75%); Skills Summary Report with **cycle chips** (Current/May 2026/Oct 2025/May 2025 — `stuCycle` shifts bars via `scoresFor`); right column: Progress-of-district-goal ring (`gpOverall` vs 75%) + (R5) the **Awaiting Verification** module, which replaced the Credential Progress card: library-list table of student-, parent- and coach-submitted activities with tabs Awaiting Verification (n) / Accepted (n) / Add Feedback, sub-skill tags, status and verified columns, pagination — visual only, no workflow (`avModule`, `AV_ROWS`, `AV_ACCEPTED_ROWS`, `avCount`, state `avTab`). Career content removed. MAV chips (E4, GM-approved drafts): summarize growth / greatest opportunity for growth / on track for Portrait of a Graduate — the latter two are new dynamic scripts. Insights use Tyler's GP-based wording. Restricted ids → lock screen |
| ~~`#/assessments`~~ | DELETED (R1) | Trend moved to Home; library moved to `#/console/assessments`; sample-questions route deleted (instrument demoed student-side). Route redirects to Home |
| ~~`#/surveys`~~ | DELETED (R1) | All career/vertical/job/interest content removed permanently. Route redirects to Home |
| `#/credentials` | Credentials (district) | R1 F1/F2 + R2 rebuild: 3 stat boxes (kept) · **Percentage of District Goal module** (R2 — replaces the points-by-competency/per-cycle/Recent Skills Builders cards: per-competency lines vs the 75% goal marker, chips All Students + 2027-2033 + Jan-Dec, up to 3 lines; months compare within one cohort in school-year order, years compare cohorts; percentages at line ends; state `pdgSel`, values `PDG_BASE`+`GP_CLASS_ADJ`+`PDG_MONTH_ADJ`, Clear all resets) · **Skills Builder Library** (`SB_LIBRARY`, tabs `sbTab`: English / Math / Social Studies / Sciences / CTE / Other / My Library). All six subjects carry Tyler's content verbatim (summary, 10 cliff notes, 5 questions with 2/1/0/0 points): English 10 Public Speaking + Chemistry Group Lab from his Credential QA doc, and Math/Social Studies/CTE/Other from his 8/24 Credential subject updates doc (Algebra I Linear Equations · Global Issues Debate · Nursing Patient Care Simulation · Community Volunteering; no Draft badges remain). Each subject tab opens with its **learning-standards list** (R2C — Tyler's slide 28/29 tables delineated: activity + 3 durable skills + Create + link per row; English Speaking/Presentation and Science Lab rows are live and open the built entry, the rest toast full-product; CTE = 11 areas × 3 activities, capped-height scroll). Entries expand (`sbOpen`) to teacher view. **My Library tab redesigned (R2): six subject rows (English/Math/Social Studies/Sciences/CTE/Other) that click through to their subject tab.** Student credential table DELETED (student data lives on the Students page). "Create Skills Builder" button → `#/credentials/checkin` |
| `#/credentials/checkin` | Create a Skills Builder | R2 rebuild to Tyler's mock (the 4-step flow, Bridge design and Farm-to-table DELETED): **Activity Information** form (Faculty Ms. Jones · Date · Subject select English 10/Chemistry · Skill Builder (Activity) · Save As), standards info banner, **Generate Teacher Cliff Notes** and **Generate Student Skills Builder Questions** cards fed verbatim from the library's English 10 + Chemistry entries (teacher view with A-D letters and points), Cancel / Save & Continue (Save opens the saved entry in the library). State `ckSubj`/`ckNotes`/`ckQs` |
| `#/benchmarks` | Benchmarks | R1 G1 rebuild — Tyler's six examples with district-wide counts (R2: first renamed **PofG-Self-management**, "Advancing" dropped per Tyler; each PofG benchmark carries a `skill` field and its detail list shows that competency in the Skills column; clicking the Students tab from Create-a-Benchmark returns to Students): PofG-Self-management (344, Whitfield) · PofG-Teamwork (496, Grant) · PofG-Creativity (813, Petrillo) · PofG-Adaptability (742, Whitfield) · PofG-Communication (628, Okonkwo) · CTE-Health Services (193, Petrillo — description verbatim from Tyler's Credential QA doc). "Create Benchmark" routes to the Students dialog. Session saves from the dialog/MAV append rows |
| `#/benchmarks/{i}` | Benchmark detail | All six clickable (G2): About card + share/PDF affordances + student table mirroring the Students-tab columns (Skills / Credentials / GP), names → staff snapshot. MAV explains the open benchmark in Tyler's template ("This benchmark includes N students … created by … with access available to …") (G3) |
| ~~`#/frameworks`~~ | DELETED (R1) | Route redirects to Home |
| ~~`#/graduate-profile`~~ | DELETED (R1) | Content now lives in the Home Graduate Profile module. Route redirects to Home |
| `#/console` | Console | Tool tiles: User Management · SIS Integration · Assessment Management · Resources & Research · Platform Tutorial Videos · AI Help Desk |
| `#/console/users` | User Management | Staff access + invitations (1 pending: James Petrillo) |
| `#/console/assessments` | Assessment Management | R2: template form (Assessment name / cycle / grad years / share) + "← Console" back link; 92-item note deleted. Create-new (cycle select + generate link, `amGen` state) + assessment library from `ASSESS_LIST` (one live link) |
| `#/console/resources` | Resources & Research | R2: the 2026 District Leaders Survey & Research White Paper links to the real PDF (filesusr.com, new tab); "← Console" back link; grounded MAV scripts (`show me the latest whitepaper` · `what is the most recent durable skills assessment?`) |
| `#/console/tutorials` | Platform Tutorial Videos | Stub — Assessments/Credentials/Benchmarks video tiles |
| `#/sis` | SIS Integration | Clever connected (nightly roster sync, 1,284 students / 2 schools); ClassLink available, not connected |

## 4. Student experience (added 2026-08-07, Tyler flow doc)

Entry paths: (a) "Create your account" on sign-on → registration → Welcome → assessment; (b) Sofia sign-in → `#/my-dashboard` hub. **MAV panel is VISIBLE for the student persona at staff depth** (R1 J2 per GM): student prompt scripts (`what do i need to do next?` · `how close am i to the credential goal?` (dynamic) · `how do i earn more credential points?` · `what does my portrait of a graduate alignment mean?` · the summer-experience feedback prompt), per-route chips/insights on every student route, and the **New Chat / Chats tabs shown to students** (`SHARED_ROUTES`; role-aware cards via `chatsFor()` / student card set in `viewNewChat`). **My Interests deleted in R1.**

| Route | Screen | What it shows / does |
|---|---|---|
| `#/student-register` | Registration overlay (pre-auth) | 3 steps: Getting Started (name/district email/grad year, district-link chip, "only your district email is eligible") → Almost Complete (verify-email inbox preview, VERIFY EMAIL) → Create Password (live checklist: 8 chars / number / uppercase; demo autofill link). Finish authenticates as Sofia → Welcome |
| `#/student-welcome` | Welcome to Student EI | Tyler's onboarding copy, 2-minute video placeholder, Next → begins the questions |
| `#/student-assessment` | Durable Skills Questions | 3 sections framed 32-30-30; three formats: Likert (6-option "Thinking about myself I tend to be: [word]"), most/least paired choice (click-to-tag, Clear button), situational judgment (scenario + statement + 5-option likelihood). Three-line no-numbers progress bar; stop-sign break screens after sections 1 and 2 ("Ask your teacher…", Save-and-finish-later); **no back button**; Next disabled until answered; congratulations screen → "My Dashboard" button lands on My Durable Skills. Demo-only jump links (Section 2 / Section 3 / Congratulations). Resume-where-left-off: Sofia's October 2026 cycle sits mid-Section 1; state in `saState` |
| `#/my-dashboard` | Student hub | R1 J2 rebuild: `sdHeadCard()` mirrors the staff header — profile + GPA (`gpaOf`), Credential Tallies (`stuTally` = `SOFIA_CRED` + Skills Builder bonus), 50%-of-GP-goal ring, Graduate Profile Alignment (8 comps, extras blue); 3 tab cards (Durable Skills / Credentials / Feedback); continue-assessment CTA and the **Skills Builder queue banner** (R2: counts both queued builders — English 10 Public Speaking and Chemistry Lab, both from Ms. Jones) at the **bottom** |
| `#/my-durable-skills` | My Durable Skills | R1 J3 rebuild: header card; cycle dropdown (May 2026 / October 2025 via `SD_DATA`; R5: moved from the page head into the Skills Summary card header to match the staff view); range-column layout with **Tyler-spec green lines 40/60/30/80/50, nothing at line ends**; R2 relabel per Tyler's option (b): definition indented under the competency name, **proficiency label at the green line's end** via `sdBucket` 20% buckets (≤20 Developing / ≤40 Emerging / ≤60 Advancing / ≤80 Excelling / else Mastering; "You are [Range]" statements removed); orange "1" nav badge (`sdNavBadge`) until the summary completes; i-icons open COMP_INFO panels. Staff student view mirrors this presentation (`staffSdWidth`) |
| `#/my-credentials` | My Credentials | R1 J4 + R2 rebuild: **dual Skills Builder queue** — English 10 Public Speaking Presentation (`ebq*` state) AND Ms. Jones Chemistry Lab (`sbq*` state), each the full 5-question set from `SB_LIBRARY` with **A-D lettered choices** (each answer awards option-points ×2 with a "Nice job! +N" toast; sub-skills roll up via `SKILL_PARENT`; separate earned maps `sbqEarned`/`ebqEarned` both feed `stuTally`); per-builder completion banners; "How credentials work" explainer; 5 per-competency cards showing `stuTally` x/500 with the 375 goal; **Skills Builder History** (`SB_HISTORY` + both queue/completed rows); nav badge counts 2→1→0 (`sbQueueCount`, kept current in `render()`) |
| `#/my-feedback` | My Feedback | R1 J5 rebuild per slide 57: "Share feedback that helps you grow" hero; three cards (Your summer / Your activities / Feedback from others — Tyler's copy); scripted MAV conversation demo (R5: ice cream stand exchange, replacing the lifeguard copy). **Mic/voice removed entirely** |

## 5. MAV (scripted assistant)

Split-screen right panel for staff (context line, insights, chips, input). Hidden for the student persona. 20 scripted prompts (`MAV_SCRIPTS`, keyed on the lowercased prompt), most navigate after replying:

`which competency needs the most attention?` · `show me the class of 2028 with emerging communication` (filters Students) · `show all students with greater than 50% alignment to our portrait of a graduate` (gpMin filter) · `clear all filters` · `overlay the last three assessments` · `show the class of 2027 only` · `who's interested in nursing careers?` (opens Healthcare/RN) · `show me the healthcare vertical` (+ legacy "health care" alias) · `tell me about the registered nurse pathway` · `tell me about the physical therapist pathway` · `show me the frameworks library` · `how aligned are we to our graduate profile?` · `is our sis connected?` · `invite a new staff member` · `save students excelling in teamwork as a portrait of a graduate benchmark` · `show our portrait of a graduate teamwork benchmark` · `which students are past the credential goal?` · `start a skills builder` (R2: opens the English 10 library entry instead of the old 4-step page) · `summarize this student's growth` (dynamic — needs a student dashboard open)

New R1 scripts: `can you show me the students who have been rostered but haven't verified yet?` (Pending filter) · `show me the students who align with the district goal in teamwork.` · `can you show me the sub-skills for self-management?` · `show me students benchmarked as developing in adaptability.` · `show me all users with district admin permissions.` (opens User Management) · `show me students meeting the district portrait of a graduate standards for communication` · `show me an example of a credentialing skills builder in a multiple-choice format for a chemistry class`. New R2 scripts: `show me the latest whitepaper` (opens Resources; PDF link) · `what is the most recent durable skills assessment?` · student-side `show me my latest durable skills` · `what is my percentage to goal for pofg?`. R2 thread behavior: New Chat cards start a fresh conversation (no duplicated history), newest exchange renders at the top, click-throughs land at the top of the page, and clicking Home resets every module to defaults and clears the thread (`resetToDefaults()`). All career scripts and the dynamic `[job] pathway` matcher are removed. Anything unmatched gets `MAV_FALLBACK`. Per-route chips and insights live in the `ROUTES` table.

## 6. Data model (invariants live in STATUS.md — that list wins)

- `STUDENTS`: 24-row roster `[first, last, gradYear, school, [SM,TW,CR,AD,CO], credPts, status, gated]` — the career `vertical` field was stripped in R1. Sofia Nguyen id 4, scores [63,67,59,61,62], 240 pts. Statuses: Active (22), Pending (Caleb Foster, Aiden Thompson).
- `COMPS`: Self-Management, Teamwork, Creativity, Adaptability, Communication. 5 proficiency ranges: Developing, Emerging, Advancing, Excelling, Mastering.
- **Portrait of a Graduate (R1):** `GP_COMPS` = COMPS + `GP_EXTRA` (Critical Thinking, Empowered Learner, Global Citizen — district-added, display blue). `gpOf(s, comp)` = per-student alignment % (Sofia fixed: 48/26/65/18/44/60/88/48; others deterministic, clamped 18–88); `gpOverall(s)`; `GP_GOAL` = 75.
- **Credential tallies (R1):** `credTally(s, comp)` = per-competency x/500 (Sofia fixed: SM 240 / TW 130 / CR 325 / AD 90 / CO 220; others derived from credPts + scores). `credTop(s)`. Invariant: exactly 4 students (Chen, Okafor, Lindqvist, Desai) have a tally ≥ `CRED_GOAL` 375.
- **GPA (R1):** `gpaOf(s)` = 4 course/grade pairs by grad year (Sofia fixed: Algebra 87, Global 11 85, Chemistry 88, English 11 93).
- **Cycles (R1):** `PERIODS` = Oct 2026 (current, live, 91% = 1,168 of 1,284), May 2026, Oct 2025, May 2025. `AGG_BASE` = Oct 2026 values; May-2026 figures now sit one `PERIOD_ADJ` step down.
- **Awaiting Verification (R5):** `AV_N` = pending-item count by `student.id % 10` (`[0,4,2,0,4,3,0,2,0,1]` — Sofia, id 4, has 4 to match Tyler's mock); `avCount(s)`, `avAccepted(s)` (1 whenever there is anything pending). `AV_ROWS` is an 8-activity pool: Tyler's four printed rows first, then four PT-authored in the same style (Part-Time Job, Performing Arts, Tutoring, Community Service) so the district queue has depth. `avPendingFor(s)` draws a contiguous run of the pool from the student's own offset — **`AV_MOCK_IDS` (Sofia id 4, Tyler Brooks id 10) are pinned to offset 0 so their records reproduce Tyler's mock exactly**, and mock records shift as a block so his printed order holds — then sorts newest first. Tyler Brooks is pinned because his own edits doc names that record ("Students tab-click student name, Tyler Brooks"); he had zero pending items until this was caught. `avShift(s,i)` / `avTime` apply a deterministic per-student, per-row day and minute offset (Sofia exempt) so no two district rows share a timestamp; `avBy(s,r)` gives a parent submitter the student's surname while faculty and coaches stay named district staff. `avDistrictQueue(n)` returns the newest n with no repeated student or activity; `avPendingTotal()` / `avStudentsPending()` drive the counts. Demo data only — there is no verification workflow behind it.
- `VERTICALS` deleted in R1 — no career data remains. `GP_DIST` (home module): SM 70 / TW 74 / CR 58 / AD 65 / CO 76 green + Critical Thinking 62 / Empowered Learner 71 / Global Citizen 67 blue; `GP_CLASS_LOW` = Class of 2029, 54%. R2 cohort shifts: `GP_CLASS_ADJ` = 2027 +4 / 2028 +2 / 2029 -8 / 2030 -2 / 2031 -3 / 2032 -4 / 2033 -5 (shared by the home GP module and the credentials PDG module).
- **Percentage of District Goal (R2):** `PDG_BASE` = TW 66 / CO 62 / SM 55 / AD 49 / CR 44; `PDG_MONTH_ADJ` school-year curve (Sep -2 → Jun +12); `pdgLines()` resolves the chip selection (months within one cohort, or cohorts by year), values clamped 12-90.
- **Proficiency buckets (R2):** `sdBucket(w)` — ≤20 Developing / ≤40 Emerging / ≤60 Advancing / ≤80 Excelling / else Mastering (no line ever exceeds 90%). Used by My Durable Skills and the staff mirror.
- **Learning standards (R2C):** `SB_STANDARDS` (Sciences/English/Social Studies/Math, 10 rows each) and `SB_STANDARDS_CTE` (11 areas × 3 = 33 rows), verbatim from Tyler's 8/24 subject-updates doc, including sub-skills beyond the core five (Problem Solving, Attention to Detail, Curiosity, Listening, Decision Making, etc.) displayed as tags. Live rows: `English:0`, `Sciences:0` via `data-stdcreate`.
- `AGG_BASE`: district competency averages by year; May-2026 "All" = 72/74/61/66/77; district goal 75.
- Credentials: 500-point scale, 375 district goal. Student-side legacy share view still uses `CRED_SHARES` until CP7; staff-side derived tallies use `credTally` (R1).
- Naming: "Portrait of a Graduate" always spelled out; MAV icon is the north star (no sparkle); no "Generated by MAV" credit lines.
- Design: `DESIGN.md` at client root — primary `#2667AA`, navy `#1A1A2E`, teal `#0097A3`, Inter; progress bars only, no radar/donut/spider; split-screen MAV panel for staff.

## 7. Changing the demo

1. Read `STATUS.md` (standing directives + invariants) and this file.
2. Back up `index.html` to the project `Archive/` before a significant change (standing rule 11).
3. Edit via Python patch scripts with exact-count asserts — template in `qa/patch_template.py`.
4. QA: `node --check` the extracted script, then run `qa/qa.js` (279 checks; see `qa/README.md`). Add checks for new behaviors; keep the hidden-student leak check and staff regression green.
5. Update this file and `STATUS.md`, commit, push `main` (or a preview branch) per the cheat sheet.

*Created 2026-08-07 by Claude as ProactiveTech GM. Reflects the demo as of the student experience build.*
